## 解决的问题与修改后的行为

<!-- 标明子项目（Word / Overleaf / 公共部分），描述用户可见变化和必要实现细节。 -->

## 验证

- [ ] 在受影响子项目目录运行 `npm test`
- [ ] 在受影响子项目目录运行 `bash -n install.sh`（涉及安装流程时）
- [ ] Overleaf 界面改动已运行 `npm run test:ui` / `npm run test:ui:cn`（如适用）
- [ ] Word 交互或格式改动已用合成文档验证（如适用）

<!-- 标明未验证的部分；不要附带真实文档、凭证或未脱敏日志。 -->

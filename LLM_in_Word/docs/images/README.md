# Screenshots / 截图说明

Captured on 2026-09-13 in **Microsoft Word for macOS**, with the locally installed LLM_in_Word 0.6.0 add-in. The demo paragraph and headings were written specifically for these screenshots. The rewrite was generated through the real local Claude Code CLI (Sonnet, medium effort).

截图于 2026-09-13 在 **macOS Microsoft Word** 内拍摄，使用本机安装的 LLM_in_Word 0.6.0。文档内容专为演示编写，模型结果由本机真实 Claude Code 生成。

| Image | What it shows / 内容 |
| --- | --- |
| `word-selection.jpg` | A selected passage, a captured target, model settings and an instruction. / 选择段落、添加目标、设置模型、输入要求。 |
| `word-diff.jpg` | The unchanged Word selection alongside the generated diff. / 原文与生成结果的差异预览。 |
| `word-tracked-changes.jpg` | The applied paragraph with Word's Review tab and Simple Markup revision indicator. / 已写入的段落、审阅功能区与待处理修订标记。 |

The last screenshot shows the tool reporting its plain-text fallback for this pasted HTML document; formatting preservation is best effort. The original demo document remains local and is not part of the repository. These images demonstrate macOS Word; Windows interactive Word testing remains a separate acceptance step.

最后一张图保留了纯文本回退提示，体现真实运行结果。演示文档仅保留在本机；仓库只包含截图。Windows Word 的交互验收仍需单独进行。

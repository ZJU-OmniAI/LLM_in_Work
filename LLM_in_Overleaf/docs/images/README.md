# Screenshot notes / 截图说明

Captured on 2026-09-25 using the actual LLM_in_Overleaf extension UI and a real CodeMirror editor in a local demo page. The source text is synthetic. The screenshots are not from the hosted Overleaf website.

截图来自本地演示页面中的真实 LLM_in_Overleaf 扩展界面和 CodeMirror 编辑器，使用专门编写的演示文稿；并非 Overleaf 官网实机截图。

The rewrite was generated by the real local Claude Code CLI (claude-sonnet-5, low effort), through the project's native host. The harness forwards browser messages to that host; it does not exercise Chrome's installed Native Messaging registration. The applied state was captured after the extension wrote the result back into CodeMirror. No personal document, account page or credential appears in the images.

改写由真实 Claude Code CLI（claude-sonnet-5，low）经本项目的本机桥生成。拍摄脚本转发浏览器消息，未覆盖 Chrome 已安装的 Native Messaging 注册链路。应用截图拍摄于扩展实际写回 CodeMirror 之后。

- **overleaf-selection.png** — select source text and enter an instruction / 选中源码并输入要求。
- **overleaf-diff.png** — review the generated diff before applying / 应用前查看真实模型改写差异。
- **overleaf-applied.png** — inspect the edited source and success state / 查看写回后的源码和成功提示。

Reproduce from this subproject with `npm ci` and `node tools/capture-docs.cjs`. Requires a signed-in Claude Code CLI and Chrome; makes one real model request and consumes quota. Set `CHROME_BIN` for a custom Chrome path.
